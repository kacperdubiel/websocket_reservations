<%- include('../header') -%>
<div class="container mt-5">
    <a href="/events/<%= event.id %>" class="btn btn-primary">Powrót</a><br/><br/>
    Client ID: <input type="text" id="clientId" value="abc123">
    <button id="reload-btn" class="btn btn-primary" onclick="loadPlaces()">Reload</button>
    <h1><%= event.name %></h1><hr/>
    <h2><%= sector.name %></h2><br/>

    <table>
        <tbody>
            <tr>
            <% for(var i = 1; i <= sector.numberOfPlaces; i++){ %>
                <% if(i % numberOfColumns === 1) { %>
                    </tr>
                    <td>
                <% } %>
                <td>
                    <div id="<%=i%>" class="place btn btn-success clickable"><%= i %></div>
                </td>
            <% } %>
            </tr>
        </tbody>
    </table>
</div>
<script src="/socket.io/socket.io.js"></script>
<script src="/static/client.js"></script>
<%- include('../footer') -%>
