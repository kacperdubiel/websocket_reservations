<%- include('../header') -%>
<div class="container mt-5">
    <a href="/events" class="btn btn-primary">Powrót</a><br/><br/>
    <h1><%= event.name %></h1><br/>

    <table class="table table-hover">
        <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Nazwa</th>
            <th scope="col">Liczba miejsc</th>
            <th scope="col"></th>
        </tr>
        </thead>
        <tbody>
        <% sectors.forEach((sector) => { %>
            <tr>
                <td><%= sector.id %></td>
                <td><%= sector.name %></td>
                <td><%= sector.numberOfPlaces %></td>
                <td><a href="<%= event.id %>/sectors/<%= sector.id %>" class="btn btn-success">Zobacz sektor</a></td>
            </tr>
        <%  }) %>
        </tbody>
    </table>

</div>
<%- include('../footer') -%>
